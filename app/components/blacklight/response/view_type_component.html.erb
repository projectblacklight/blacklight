<div class="view-type">
  <span class="visually-hidden"><%= t('blacklight.search.view_title') %></span>
  <div class="view-type-group btn-group">
    <% views.each do |view| %>
      <%= view %>
    <% end %>
  </div>
</div>
