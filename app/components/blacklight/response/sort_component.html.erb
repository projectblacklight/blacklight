<%= helpers.render(Blacklight::System::DropdownComponent.new(
      param: @param,
      choices: @choices,
      id: @id,
      classes: @classes,
      search_state: @search_state,
      selected: @selected)) %>
