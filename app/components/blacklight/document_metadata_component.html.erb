<dl class="document-metadata dl-invert row">
  <% @fields.each do |field| -%>
    <%= helpers.render(field_component(field).new(field: field, show: @show, view_type: view_type)) %>
  <% end -%>
</dl>
