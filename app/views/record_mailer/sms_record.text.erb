<% @documents.each do |document| %>
<%= document.to_sms_text %>
<%= t('blacklight.record_mailer.sms.url', :url => polymorphic_path(document, {:only_path => false}.merge(@url_gen_params)) ) %>
<% end %>
