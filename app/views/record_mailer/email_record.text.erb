<% @documents.each do |document| %>
<%= document.to_email_text %>
<%= t('blacklight.record_mailer.email.url', :url =>polymorphic_path(document, {:only_path => false}.merge(@url_gen_params))) %>
<% end %>
<%= t('blacklight.record_mailer.email.message', :message => @message) %>
