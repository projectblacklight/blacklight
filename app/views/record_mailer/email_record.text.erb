<% @documents.each do |document| %>
<%= document.to_email_text %>
<%= t('record_mailer.email.url', :url =>polymorphic_path(document, {:only_path => false}.merge(@url_gen_params))) %>
<% end %>
<%= t('record_mailer.email.message', :message => @message) %>
