<%= link_to action_label(action_name, document_action_config), action_path(action_name, document_action_config, id: document_or_list), id: document_action_config.fetch(:id, "#{action_name}Link"), data: {}.merge(({ajax_modal: "trigger"} if document_action_config.modal != false) || {}) %>