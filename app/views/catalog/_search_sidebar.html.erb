<%# this partial is deprecated.  catalog/index now renders the component %>
<% conf = blacklight_config_presenter.view_config %>
<%= render conf.sidebar_component.new(blacklight_config: blacklight_config,
                                      response: @response,
                                      view_config: conf) %>
