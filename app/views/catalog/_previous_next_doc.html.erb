<% if @previous_document || @next_document %>
	<div id="previousNextDocument">
		<%= raw(link_to_previous_document(@previous_document)) %>
		<%= raw(link_to_next_document(@next_document)) %>	
	</div>
<% end %>
